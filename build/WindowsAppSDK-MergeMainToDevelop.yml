name: $(BuildDefinitionName)_$(date:yyMM).$(date:dd)$(rev:rrr)
jobs:
- job: Merge
  pool: $(ProjectReunionBuildPool)
  steps:
  - checkout: self
    persistCredentials: true

  - task: PowerShell@2
    inputs:
      targetType: 'inline'
      script: |
        git checkout TestDevelop
        git config user.name "reunion-maestro-bot"
        git config user.email "reunion-maestro-bot@microsoft.com"
        git pull origin main
        git push > log.txt 2> $(Build.SourcesDirectory)\errorlog.txt
        $error = Get-Content  $(Build.SourcesDirectory)\errorlog.txt
        Write-Host "ERRRIRS"
        Write-Host $error