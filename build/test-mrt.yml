steps:
- task: DownloadBuildArtifacts@0
  inputs:
    artifactName: "MrtCoreManagedTest-$(buildPlatform)-$(buildConfiguration)"
    downloadPath: '$(Build.SourcesDirectory)'

- task: DownloadBuildArtifacts@0
  inputs:
    artifactName:  "MrmUnitTest-$(buildPlatform)-$(buildConfiguration)"
    downloadPath: '$(Build.SourcesDirectory)'

- task: DownloadBuildArtifacts@0
  inputs:
    artifactName: "MrmBaseUnitTests-$(buildPlatform)-$(buildConfiguration)"
    downloadPath: '$(Build.SourcesDirectory)'

- task: DownloadBuildArtifacts@0
  inputs:
    artifactName: "MrtCoreUnpackagedTests-$(buildPlatform)-$(buildConfiguration)"
    downloadPath: '$(Build.SourcesDirectory)'
  
# Run the test locally on the Azure VM.
- task: VSTest@2
  displayName: 'test MRT: ManagedTests'
  inputs:
    testSelector: 'testAssemblies'
    testAssemblyVer2: '**\MrtCoreManagedTest.build.appxrecipe'
    searchFolder: '$(Build.SourcesDirectory)\MrtCoreManagedTest-$(buildPlatform)-$(buildConfiguration)'
    testRunTitle: 'test MRT: ManagedTests - $(buildPlatform)'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: VSTest@2
  displayName: 'test MRT: UnitTests'
  inputs:
    testSelector: 'testAssemblies'
    testAssemblyVer2: '**\MrmUnitTest.dll'
    searchFolder: '$(Build.SourcesDirectory)\MrmUnitTest-$(buildPlatform)-$(buildConfiguration)'
    testRunTitle: 'test MRT: UnitTests - $(buildPlatform)'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: VSTest@2
  displayName: 'test MRT: BaseUnitTests'
  inputs:
    testSelector: 'testAssemblies'
    testAssemblyVer2: '**\MrmBaseUnitTests.dll'
    searchFolder: '$(Build.SourcesDirectory)\MrmBaseUnitTests-$(buildPlatform)-$(buildConfiguration)'
    testRunTitle: 'test MRT: BaseUnitTests - $(buildPlatform)'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'

- task: VSTest@2
  displayName: 'test MRT: UnpackagedTests'
  inputs:
    testSelector: 'testAssemblies'
    testAssemblyVer2: '**\MRTCoreUnpackagedTests.dll'
    searchFolder: '$(Build.SourcesDirectory)\MrtCoreUnpackagedTests-$(buildPlatform)-$(buildConfiguration)'
    testRunTitle: 'test MRT: UnpackagedTests - $(buildPlatform)'
    platform: '$(buildPlatform)'
    configuration: '$(buildConfiguration)'
