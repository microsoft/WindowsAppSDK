steps:
  - template: WindowsAppSDK-BuildProject-Steps.yml
    parameters:
      solutionPath: WindowsAppSDK.sln
      nugetConfigPath: nuget.config
      buildOutputDir: $(buildOutputDir)
      publishDir: $(publishDir)
