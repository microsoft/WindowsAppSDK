## Build

- `dotnet build` — compile.
- No `dotnet run`: class libraries have no entry point. Reference from a WinUI 3 app or unit-test project (`dotnet new winui-unittest`).

See [Class libraries](https://learn.microsoft.com/dotnet/standard/class-libraries).
