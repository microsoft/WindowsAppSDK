## Build and run tests

- VS: *Test → Test Explorer → Run All Tests*.
- CLI: `dotnet test` (with `--filter "FullyQualifiedName~..."` or `"TestCategory=Smoke"`).

See [Filter MSTest tests](https://learn.microsoft.com/dotnet/core/testing/selective-unit-tests).
