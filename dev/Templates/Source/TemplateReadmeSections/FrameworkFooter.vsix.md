*Generated from the Visual Studio Windows App SDK C# project templates.*

To update the templates, update Visual Studio to its latest version, or use
the Visual Studio Installer to update the Windows App SDK C# Templates
component.

To browse the full set of WinUI templates installed in Visual Studio, open
*File → New → Project* and filter by the *WinUI* language tag, or run
`dotnet new list winui` from a Developer PowerShell to see the parallel
CLI templates.
